apiVersion: v1
kind: Service
metadata:
  name: nomad-ctrl-plane-svc
spec:
  ports:
    - name: grpc
      port: 50051
      appProtocol: kubernetes.io/h2c
      targetPort: grpc
  selector:
    app: nomad-ctrl-plane
